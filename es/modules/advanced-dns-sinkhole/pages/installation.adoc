= Instalación
include::{partialsdir}/attributes-es.adoc[]
:guidename: advanced-dns-sinkhole

include::{partialsdir}/install-xlist.adoc[leveloffset=1]

include::{partialsdir}/install-dns.adoc[leveloffset=1]

include::{partialsdir}/install-event.adoc[leveloffset=1]

include::{partialsdir}/install-archive.adoc[leveloffset=1]

TIP: Si está instalando en una Raspberry debe definir la arquitectura al lanzar el instalador con `sudo ARCH=arm64 ./installer.sh`. Si desea instalar el software en alguna otra arquitectura, las arquitecturas disponibles son: `amd64`, `arm`, `arm64`, `mips`, `mips64`, `mips64le`, `ppc64` y `s390x`.

== Instalación de mongodb

[source,bash]
----
sudo apt install mongodb-server
----

ifdef::env-site,env-github[]
A continuación: xref:configuration.adoc[Configuración]
endif::env-site,env-github[]
